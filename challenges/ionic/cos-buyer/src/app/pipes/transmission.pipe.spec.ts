import { TransmissionPipe } from './transmission.pipe';

describe('TransmissionPipe', () => {
  it('create an instance', () => {
    const pipe = new TransmissionPipe();
    expect(pipe).toBeTruthy();
  });
});
